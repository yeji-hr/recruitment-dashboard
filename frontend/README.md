# 채용 대시보드 - Frontend

Next.js 14 + TypeScript + Tailwind CSS로 구축된 채용 관리 대시보드입니다.

## 🛠 기술 스택

- **Framework**: Next.js 14 (App Router)
- **Language**: TypeScript
- **Styling**: Tailwind CSS
- **State Management**: TanStack Query (React Query)
- **HTTP Client**: Axios
- **Icons**: Lucide React
- **Date Utils**: date-fns

## 📁 프로젝트 구조

```
frontend/
├── app/                      # Next.js App Router
│   ├── layout.tsx           # 루트 레이아웃
│   ├── page.tsx             # 대시보드 메인
│   ├── candidates/          # 지원자 관련 페이지
│   ├── pipeline/            # 채용 파이프라인
│   └── settings/            # 설정
├── components/              # React 컴포넌트
│   ├── ui/                  # 공통 UI 컴포넌트
│   ├── layout/              # 레이아웃 컴포넌트
│   ├── dashboard/           # 대시보드 컴포넌트
│   ├── candidates/          # 지원자 컴포넌트
│   └── pipeline/            # 파이프라인 컴포넌트
├── hooks/                   # 커스텀 훅
├── services/                # API 서비스
├── types/                   # TypeScript 타입 정의
├── utils/                   # 유틸리티 함수
└── providers/               # Context Providers
```

## 🚀 시작하기

### 1. 의존성 설치

```bash
npm install
```

### 2. 환경 변수 설정

`.env.local` 파일을 생성하고 다음 내용을 추가하세요:

```env
NEXT_PUBLIC_API_URL=http://localhost:5000/api
```

### 3. 개발 서버 실행

```bash
npm run dev
```

브라우저에서 [http://localhost:3000](http://localhost:3000)을 열어 확인하세요.

## 📄 주요 페이지

### 1. 대시보드 (`/`)
- 채용 통계 현황
- 단계별 지원자 수
- 최근 지원자 목록

### 2. 지원자 관리 (`/candidates`)
- 지원자 목록 조회
- 검색 및 필터링
- 지원자 추가/수정/삭제

### 3. 지원자 상세 (`/candidates/[id]`)
- 지원자 상세 정보
- 상태 변경
- 이력서/포트폴리오 링크

### 4. 채용 파이프라인 (`/pipeline`)
- 칸반 보드 형식
- 단계별 지원자 현황

## 🎨 컴포넌트

### UI 컴포넌트
- `Button` - 버튼 컴포넌트
- `Input` - 입력 필드
- `Select` - 선택 박스
- `Modal` - 모달 창
- `Card` - 카드 컨테이너
- `Table` - 테이블

### 지원자 컴포넌트
- `CandidateTable` - 지원자 목록 테이블
- `CandidateForm` - 지원자 등록/수정 폼
- `StatusBadge` - 상태 뱃지
- `FilterBar` - 검색/필터 바

## 🔧 커스텀 훅

- `useCandidates` - 지원자 목록 조회
- `useCandidate` - 지원자 상세 조회
- `useCreateCandidate` - 지원자 생성
- `useUpdateCandidate` - 지원자 수정
- `useDeleteCandidate` - 지원자 삭제
- `useStats` - 통계 조회

## 📦 빌드

```bash
npm run build
```

## 🔍 린트

```bash
npm run lint
```

## 📝 참고사항

- 현재는 Mock 데이터로 동작합니다
- 백엔드 API 구현 후 실제 데이터 연동이 필요합니다
- React Query를 사용하여 서버 상태를 관리합니다

## 🎯 다음 단계

1. 백엔드 API 연동
2. 실시간 업데이트 (WebSocket)
3. 파일 업로드 기능
4. 드래그 앤 드롭 파이프라인
5. 다크 모드 지원

---

**Made with ❤️ using Next.js & Tailwind CSS**

